let v = 5
let u = '5'

console.log(v === u); // strictly checks the equality 
console.log(v == u); // checks for equality regardless of the datatype
console.log(v != u); // loosely checks for inequality
console.log(v !== u); // strict non-equality

// also working with the COMPARISON operators > < >= <=
// also working with the LOGICAL operators 
// or: ||
// and: && 
// not: !
// remove the first item and the last item of the array: shift(); first and pop(); last

// add 'FIRST' element to the start of the array: unshift()

// Assign "hello world" to fourth item value: splice(3, 1, "hello world")

// Assign "MIDDLE"  to the third index value: items[2] = "MIDDLE"

// Add LAST  to the last position in the array: push("LAST")

// Output to the console: console.log(items)
